Run mvn_build.sh compile the jar file, then run upload_jar.sh to deploy it on AWS lambda.
Make sure to run the set up in the aws-lambda-java-base-layer to set up the dependencies 
needed by this code to run on AWS.
